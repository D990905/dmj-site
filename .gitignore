# macOS FUSE mount stale-handle artifacts
.fuse_hidden*

# macOS metadata
.DS_Store
._*

# Editor / OS
*.swp
*~

# Phase Overnight Final — dead weight, do NOT deploy:
# These folders contain emptied raw render PNGs / page renders.
# They are size 0 in this repo but should be excluded from any deploy.
assets/images/_raw/
assets/images/products/*/_raw/

# Compression temp files (ImageMagick `cp` artifacts that couldn't be removed
# due to mount permissions; they are size 0 and harmless but excluded from deploy):
**/*.jpg.new
**/.test
**/test2.txt

# sed -i.bak artifacts — iCloud mount 가 .bak 파일 즉시 삭제 불가 (Operation not permitted)
# 본인 cleanup 가능 시 rm 권장, 그 전까지 deploy 차단 (Alex Park #4 · 2026-06-02)
**/*.bak
**/*.html.bak

# §209 (Alex Park #4 · 2026-06-06) — _ci_site/ = CI build cache (pr-check.yml rsync staging).
# 105 file 누적 = local-only build artifact. push 안 함. git status 정돈.
_ci_site/
